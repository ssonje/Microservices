server:
    port: 8086

spring:
  application:
    name: RATING-TESTS-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/ratingmicroservicestest

  config:
    import: configserver:http://localhost:8085

server:
    port: 8086

spring:
  application:
    name: HOTEL-TESTS-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/hotelmicroservicestest

  config:
    import: configserver:http://localhost:8085
